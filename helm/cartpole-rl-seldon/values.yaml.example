# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# COMBINER, TRANSFORMER, ROUTER
oauth_key: oauth_key
oauth_secret: oauth_secret
config:
  type: MODEL
  image:
    tag: "0.2.1"

